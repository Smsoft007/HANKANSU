<aside id="left">
  <h1><img src="img/logo_w.png" alt="한강수병원" /></h1>
  <p class="info">접속자 정보</p>
  <nav class="gnb">
    <a href="notice" data-link="#notice">공지사항</a>
    <a href="health" data-link="#health">건강진단 사전조사</a>
    <a href="diagnosis_result" data-link="#diagnosis_result">건강진단 결과</a>
    <a href="quotation_inquiry" data-link="#quotation_inquiry">견적서 조회</a>
    <a href="other_data" data-link="#other_data">기타 보건자료</a>
    <a href="detailed_inquiry" data-link="#detailed_inquiry">결과 상세조회</a>
    <a href="other_health" data-link="#other_health">기타 보건자료</a>
    <a href="examination" data-link="#examination">검진요청</a>
  </nav>
</aside>

<script>
  // 1. 모든 섹션 요소들과 메뉴아이템들을 가지고 온다
  // 2. IntersectionObserver를 이용해서 모든 섹션들을 관찰한다
  // 3. 보여지는 섹션에 해당하는 메뉴 아이템을 활성화 시킨다
  const sectionIds = [
    '#notice',
    '#health',
    '#diagnosis_result',
    '#quotation_inquiry',
    '#other_data',
    '#examination',
    '#detailed_inquiry',
    '#other_health',
    '#examination',
  ];

  const cur_path = window.location.pathname;
  let rep_path = cur_path.replace('/', '');
  console.log(rep_path);
  const sections = sectionIds.map((id) => document.querySelector(id));
  const navItems = sectionIds.map((id) =>
    document.querySelector(`[data-link="${id}"]`)
  );
  let selectedNavIndex = 0;
  let selectedNavItem = navItems[0];
  function selectNavItem(selected) {
    selectedNavItem.classList.remove('active');
    selectedNavItem = selected;
    selectedNavItem.classList.add('active');
    console.log(rep_path);
    location.href(rep_path);
  }
</script>
